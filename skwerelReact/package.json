{
  "name": "skwerelreact",
  "version": "1.0.0",
  "description": "App that squirrels away info about movies and tv which later can be used to notify user",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "gHgraveDesigns",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "axios": "^0.21.2",
    "cheerio": "^1.0.0-rc.3",
    "express": "^4.17.1"
  }
}
